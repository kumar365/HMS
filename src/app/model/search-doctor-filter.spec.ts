import { SearchDoctorFilter } from './search-doctor-filter';

describe('SearchDoctorFilter', () => {
  it('should create an instance', () => {
    expect(new SearchDoctorFilter()).toBeTruthy();
  });
});
